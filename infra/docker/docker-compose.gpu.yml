services:
  worker:
    environment:
      MODEL_BACKEND: diffsynth_qwen
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
