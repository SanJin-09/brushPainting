services:
  worker:
    environment:
      MODEL_BACKEND: diffsynth_qwen
      SAM3_BACKEND: sam3
      SAM3_PRELOAD: "false"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
